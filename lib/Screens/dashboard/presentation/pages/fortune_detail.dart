import 'package:flutter/material.dart';

class FortuneDetail extends StatelessWidget {
  const FortuneDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
